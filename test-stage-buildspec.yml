version: 0.2

phases:

  install:
    runtime-versions:
      nodejs: 20
  pre_build:
    commands:
      - echo "Node Version"
      - node -v
      - npm -v
      - cd api
      - npm install
  build:
    commands:
      - npm run test
      - cd ..